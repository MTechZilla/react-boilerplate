import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  // fonts: {
  //   heading: "Inter",
  //   body: "Inter",
  // },
  colors: {
    primary: "#0F5FCA",
  },
});
export default theme;
